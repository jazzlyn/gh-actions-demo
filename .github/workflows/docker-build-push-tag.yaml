---
name: docker-build-push-tag

on:
  push:
    tags:
      - "v*.*.*"
      - "*.*.*"

jobs:
  docker-build-push:
    permissions:
      contents: read
      id-token: write
      packages: write
    uses: strg-at/github-workflows/.github/workflows/docker-build-push-github.yaml@aae8aab618b35545330c67df95174bf7983b9a08
    with:
      runner: '["ubuntu-latest"]'
      subpath: app-tag
      file: app-tag/Dockerfile
      context: app-tag
      tags: |
        type=ref,event=tag,priority=300
        ${{ github.sha }}
        type=raw, value=rolling
