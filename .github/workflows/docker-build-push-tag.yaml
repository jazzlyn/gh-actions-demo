---
name: docker-build-push-tag

on:
  push:
    tags:
      - "v*.*.*"
      - "*.*.*"

jobs:
  docker-build-push:
    permissions:
      contents: read
      id-token: write
      packages: write
    uses: strg-at/github-workflows/.github/workflows/docker-build-push-github.yaml@7649d1ef9f1bf0d146b110ffcbe9bb476d1f4d68
    with:
      runner: '["ubuntu-latest"]'
      subpath: app-tag
      file: app-tag/Dockerfile
      context: app-tag
      tags: |
        type=ref,event=tag,priority=300
        ${{ github.sha }}
        type=raw, value=rolling
