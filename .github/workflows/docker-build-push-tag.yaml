---
name: docker-build-push-tag

on:
  push:
    tags:
      - "v*.*.*"
      - "*.*.*"

jobs:
  docker-build-push:
    permissions:
      contents: read
      id-token: write
      packages: write
    uses: strg-at/github-workflows/.github/workflows/docker-build-push-github.yaml@1d739fed29e14a63caad8e41b41548ca48f7980b
    with:
      runner: '["ubuntu-latest"]'
      subpath: app-tag
      file: app-tag/Dockerfile
      context: app-tag
      tags: |
        type=ref,event=tag,priority=300
        ${{ github.sha }}
        type=raw, value=rolling
