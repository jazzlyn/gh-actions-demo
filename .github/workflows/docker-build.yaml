---
name: docker-build

on:
  pull_request:
    types:
      - opened
      - synchronize
    paths-ignore:
      - .github/**

jobs:
  docker-build:
    uses: "strg-at/github-workflows/.github/workflows/docker-build.yaml@495f55328e4384bbe19d9e06b92589401bd9c5b5" # v1.3.2
    with:
      runner: '["ubuntu-latest"]'
