---
name: docker-build

on:
  pull_request:
    types:
      - opened
      - synchronize
    paths-ignore:
      - .github/**

jobs:
  docker-build:
    uses: "strg-at/github-workflows/.github/workflows/docker-build.yaml@v1.3.2"
    with:
      runner: '["ubuntu-latest"]'
