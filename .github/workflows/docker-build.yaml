---
name: docker-build

on:
  workflow_dispatch:
  # pull_request:
  #   types:
  #     - opened
  #     - synchronize
  #   paths-ignore:
  #     - .github/**

jobs:
  docker-build:
    uses: "strg-at/github-workflows/.github/workflows/docker-build.yaml@ff86b2cf06e38a4cbc0afaaf0427a9c2549e4a62" # v1.5.2
    with:
      runner: '["ubuntu-latest"]'
