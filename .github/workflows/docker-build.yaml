---
name: docker-build

on:
  workflow_dispatch:
  # pull_request:
  #   types:
  #     - opened
  #     - synchronize
  #   paths-ignore:
  #     - .github/**

jobs:
  docker-build:
    uses: "strg-at/github-workflows/.github/workflows/docker-build.yaml@v1.5.1"
    with:
      runner: '["ubuntu-latest"]'
